# 瓷砖文案生成器 — 交接文档

> 本窗口（2026-08-10，第二轮）会话结束交接。接手前先读本文件 + `git log`，关键代码结构见第 3 节。
> 用户是**深圳 AI 应用开发者**，偏好**中文交流**、**先讨论再开工**（动代码前先讲方案确认）。

---

## 1. 项目是什么

一个**文案风格生成器**：上传参考样本文本 → LLM 分析提炼"作者画像" → 前端按三种文案类型（小红书/朋友圈/销售话术）× 两种风格模式生成文案，SSE 流式输出。

- 技术栈：FastAPI + SSE 流式 + LangChain（`langchain-openai`）+ DashScope 阿里云百炼兼容接口
- 运行环境：`rag_env/Scripts/python`（venv），主模型 **deepseek-v4-pro**（.env `LLM_MODEL_NAME` 切换；qwen-plus / deepseek-v4-flash 都试过并被换掉，对比见 4.3），快模型 `qwen-turbo`（config 里已定义，未实际使用）
- 启动：`rag_env/Scripts/python -m uvicorn app:app --port 8000`，访问 http://127.0.0.1:8000
- 前端是**静态文件**，改 `static/index.html` 刷新页面即生效，**不用重启服务**；改 Python 才需要重启
- ⚠️ 本窗口完成**画像 v2 重构**（可执行画像 + 两档模式），详见第 5 节

## 2. 目录结构

```
personalaitext/
├── app.py            # FastAPI 入口 + SSE 路由（上传/画像/主题/生成）
├── generator.py      # 核心生成逻辑：场景池、三种类型要求、两档风格模式、主题层、子类型均衡抽示例
├── style_manager.py  # 样本管理 + 画像 v2 构建（LLM 一次调用 + 程序统计 → style/tile_style.json）
├── llm.py            # LLM 封装（ChatOpenAI 直连 DashScope 兼容接口）
├── config.py         # 路径、模型、API key 读取
├── data/             # 上传的参考样本文本（.txt/.md），当前 41 条插画师朋友圈文案
│   └── _archive/     # 已移出的参考文档（归档，不真删、可恢复，见 5.4）
├── style/tile_style.json  # 画像 v2（可执行数据；构建产物，git 已忽略）
├── static/index.html      # 前端单页
├── requirements.txt
├── .env              # ⚠️ 含 DASHSCOPE_API_KEY，绝不上传 git、绝不读取内容
└── .gitignore        # 已排除 .env / 缓存 / 临时文件
```

## 3. 核心架构与职责（当前代码为准）

### 3.1 三层数据流

**样本池 → 风格画像 → 生成 prompt**
- 样本池 `_example_pool`：`data/` 里所有文本按 **2 个以上空行**切分为条目（`style_manager._split_entries`，本窗口从 generator 移过来避免循环导入），洗牌轮换抽取，一轮内不重复
- **子类型均衡抽取** `_pick_by_subtype`：画像带 `subtype` 时，按类配额抽 6 条（每类按条数均分、余数给多的类），相邻两条不同类，类内一轮不重复；无画像/画像旧格式时退回全池洗牌
- 画像 `style/tile_style.json`：`style_manager.build_style()` 生成，**一次 LLM 调用**输出全部画像字段 + 85 条样本的子类型分类（编号→类型，程序校验全覆盖、失败重试一次），`length` 由程序统计

### 3.2 两种风格模式（`generator.STYLE_MODES`，画像 v2 重构后缩成两档）

| 模式 | 内容 | 温度 | 说明 |
|---|---|---|---|
| `style` | 画像 v2 全量 + 6 条子类型均衡示例 | 0.7 | **默认、推荐** |
| `none` | 无画像 + 6 条全池示例 | 1.0 | 对照实验用 |

两个模板 `PROMPT_STYLE_TMPL` / `PROMPT_NONE_TMPL` 共用公共创作要求常量 `CREATION_REQUIREMENTS`（历次调优约束，改动一处两模板生效，不再三处同步）。

### 3.3 无主题时的兜底：场景池 / 纯感悟模式（`OPENING_SCENES`）

- **小红书、销售话术**无主题：代码随机抽一个场景注入 prompt（`_pick_scene`，洗牌轮换一轮不重复），内容只写"我做了什么"，不写他人评价
- **朋友圈无主题：不注入场景，走"纯感悟"模式**——直接写一段道理/感悟，主体是观念和感悟，最多带一句经营日常做引子，禁编事件流水账；主题面要宽（信任/坚持/品质/时节/节日/感恩/自嘲都可以，换着角度写）

### 3.4 三种文案类型（`generator.TYPES`）

- `xiaohongshu`：小红书种草（标题+正文+话题标签，200-400 字）
- `wechat_moment`：**朋友圈格言体**（重点调优对象，要求最细，80-130 字）
- `sales_script`：销售话术（五段式，300-500 字）

### 3.5 主题层（`generator.build_topic` + `/api/build_topic`）

两步式：前端输入"今天干了什么" → LLM 整理成 **15-30 字口语白描主题**（可编辑）→ 生成时以主题为场景核心。主题为空则走场景池兜底。关键约束：只整理原话、**禁止脑补细节**、禁他人评价。

### 3.6 身份动态化（`_get_identity`）

- identity 由**用户前端输入行业**拼接（不再让 LLM 提炼），如"在造纸行业摸爬滚打多年的老手……"
- 拼接时清洗：去掉"你是一位"前缀、剥掉句尾标点

### 3.7 API 一览（`app.py`）

| 接口 | 方法 | 作用 |
|---|---|---|
| `/` | GET | 前端页面（带 `Cache-Control: no-cache`，开发期避免缓存旧页面） |
| `/api/status` | GET | 样本数 + 画像是否存在 + 画像全文 + `archived` 归档列表 |
| `/api/upload` | POST | 上传样本（.txt/.md） |
| `/api/build_style` | POST | 生成画像 v2，body `{industry}` |
| `/api/build_topic` | POST | 主题层，body `{text}` |
| `/api/generate` | POST | SSE 生成，body `{type, style_mode, topic}` |
| `/api/sample` | DELETE | **移除样本**（移入 `data/_archive/`，不真删；清示例池缓存；移除后 0 样本则清画像） |
| `/api/archived` | GET | 列出归档样本 |
| `/api/restore` | POST | 把归档样本移回 `data/`（同名冲突时拒绝），body `{name}` |
| `/api/reset` | DELETE | 清空样本 + 画像 |

## 4. 开发历程与调优结论（重要！都是血泪教训）

### 4.1 "人味不够重"的根因（A/B 实验）

**画像越抽象，人味越少；示例原文才是人味载体。** 完整 7 维画像把风格写成了"描述文档"，模型照着抄反而没味道。所以画像 v2 改为**可执行数据**（模板+原句），示例保持加量。

### 4.2 历次风格调优（每个问题的根因 + 修复，全部已写进 prompt）

| 用户反馈 | 根因 | 修复 |
|---|---|---|
| 主题太长有续写 | 分析 prompt 写了"可补充合理细节"，模型脑补剧情 | 改为只整理原话、15-30 字、禁编细节，加 few-shot |
| "客户满意/师傅夸"评价腔 | 场景池带评价表述 + 模型营销先验 | 场景池白描化；主题 prompt 禁他人评价；模板加"感悟只写自己" |
| 对话台词 / "你…我…"对仗金句 | 约束不够强、分散 | 模板统一加"全程第一人称独白、禁借人说话、禁对'你'喊话、禁对仗金句" |
| 写日记的感觉 | 参考文档是**格言体/宣言体**，生成是叙事体流水账 | 朋友圈要求重写为格言体 8 点：观念断言为主体、事件只做引子 |
| 每篇都"搬砖人"收尾 | 我把"加油，搬砖人"写进 prompt 当示例 | 改为"收尾方式要多样" |
| 身份 LLM 提炼效果差 | LLM 提炼不稳 | 改为用户输入行业拼接 |
| 换行业出现"砖" | 样本本身是瓷砖文案（示例池实时从样本抽），非代码问题 | 用户确认问题不大，未改代码 |
| "不是…而是…"每篇必用 | 3 层 prompt 硬编码 + qwen 强先验 | 弱化措辞 + 频率数字 + 低频标注 + 模板后置，压到多数篇 0 次；**换 deepseek-v4-pro 后 5 条全达标**（见 4.3） |
| 生成主题趋同（总写信任/晚归，"晨光破云开"开头连撞） | qwen 对"朋友圈营销"先验极强，软指令压不过；我的 prompt 还把"信任"当唯一示范词反复出现 | ① prompt 摘"信任"唯一示范位 →"主题面要宽"列举多主题；② tone_anchors 手动改为主题分散 3 条 + 分析 prompt 加"主题必须分散"；③ 示例抽取加信任浓度检查 `_trust_ratio`（>50% 重抽，最多 5 轮）；④ 最终靠**换模型**解决（见 4.3） |
| 前端看不到删除按钮 | ①浏览器缓存旧 index.html（改动后不刷新永远看不到新功能）②删除按钮 CSS 用 `position:absolute` 但卡片 `.tile` 没设 `position:relative`，按钮脱离卡片被甩到 body 角落 | ① `/` 接口加 `Cache-Control: no-cache` ② `.tile` 加 `position:relative`，按钮改陶土橙圆角小按钮（橙底+描边，悬停实心） |

### 4.3 换模型实测结论（2026-08-10，用户拍板留 deepseek-v4-pro）

无主题朋友圈同 prompt 连测 5 条，同一 DashScope key 换模型名（`LLM_MODEL_NAME` 环境变量）：

| | qwen-plus（原） | deepseek-v4-flash | **deepseek-v4-pro（现用）** |
|---|---|---|---|
| "不是…而是…" | 3/5 超标 | 4/5 达标 | **5/5 达标** |
| 主题多元 | 全信任/晚归 | 全信任 | **多元**（感悟/坚持/晚归/手艺/信任各一） |
| 篇幅 | 140-175 超样本 | 78-109 贴样本 | 74-113 贴样本 |
| 开头 | "晨光破云开"×4 | 不撞车 | 不撞车 |
| 照抄锚点原句 | 无 | 整段抄×2 | 无 |
| 口语自然度 | 一般 | 好 | **最好**（"说不累是假的，但心里踏实"） |
| 速度 | 快 | 快 | **慢**（性能档，用户接受） |

结论：qwen-plus 对"朋友圈营销"先验太强（信任/晨光/排比），prompt 压不动；flash 口语好但主题仍趋同且会整段抄 tone_anchors；**pro 全达标**，已写入 .env 固化。主题池方案（代码轮换指定感悟主题）设计过但未实现——pro 自己就能写开，暂不需要。

**模板里沉淀的关键约束（动 prompt 时别删）：**
- 文案以经营日常/真实感受/人情味为主，不堆产品卖点
- 感悟只写自己（今天做了什么、身体和心里状态），不替客户/师傅下结论，禁"客户很满意""师傅夸"
- 观念和感悟为主体，当天的事只做一两句引子，不写事件流水账；**格言体**判断句（不是……而是……/……才是……）
- 全程第一人称独白：禁借任何人之口说话（师傅说/客户说）、禁对"你"喊话、禁"你…我…"对仗金句
- 产品只用泛称（这批砖/咱家的砖/今天送的那车货），绝不出现具体产品名/系列名/花色名/型号
- 只输出文案正文，不要解释说明

## 5. 当前状态：画像 v2 已上线（本窗口完成）

### 5.1 画像 v2 schema（`style/tile_style.json`，可执行数据）

| 字段 | 生产方式 | 形态 |
|---|---|---|
| `identity` | 用户输入行业拼接 | 角色句（不动） |
| `structure` + `structure_example` | LLM | 骨架字符串 + 原句例 |
| `openings` / `core_templates` / `closings` | LLM | **三段句库**，每段 `{pattern, example}`（开头/观念主体/收尾），example 必须来自样本原句 |
| `imagery` | LLM | `{domain, examples}` 意象库 |
| `tone_anchors` | LLM | 3 句最有味道的原句，**主题必须分散**（信任/坚持/时节各一，已手动改过当前画像 + 分析 prompt 加硬要求） |
| `humor` | LLM | `{pattern, example}` 幽默套路（本窗口新加，用户拍板"做看看效果"） |
| `avoid` | LLM | 避免项 |
| `length` | **程序** | min/median/max |
| `subtype` | LLM 分类+程序校验 | 类→原句列表（状态型/观念型/感恩型/自嘲型），85 条全覆盖 |

**被用户否掉的项（别再加回来）：**
- ❌ 高频词/主题词统计（`high_freq_words`）：样本内容太少统计的是噪音；把词写进 prompt 等于反向强化，与"稀释"目的相悖。

### 5.2 本窗口产出

- **样本扩充**：用户加内容后 86 条 → 删完全重复 1 条 → **85 条**（14~101 字，中位数 50）。用户拍板：**"客户满意就是我搬砖的最大动力"这类样本保留**（出现频率低，模型学到的就是"偶尔出现"，prompt 硬约束兜底）
- **画像 v2 真实效果**（2026-08-10 15:02 生成）：结构骨架+原句例 ✓、三段句库（开头10/观念10/收尾7）✓、意象6、锚点3、**humor 提炼质量很高**（"四舍五入伪逻辑""荒诞类比+反差自嘲"）✓、子类型 85 条全覆盖（状态14/观念48/感恩17/自嘲6）✓、篇幅 14-50-101 ✓
- **连测结果**：朋友圈 style/none 两档都贴风格（"不是…而是…"判断句 + 信任主题 + 感谢收尾 `[握手][握手]`）；小红书种草文细节生动；销售话术五段式完整、产品只用泛称
- **无主题纯感悟模式**（本轮新增）：朋友圈无主题不再注入场景，直接写道理/感悟（用户："参考文档里也有很多是没主题的道理感悟"）；小红书/话术仍走场景池
- **主模型换为 deepseek-v4-pro**（.env 固化），详见 4.3

### 5.4 换参考文档 = 换风格（验证成立）+ 归档功能（本窗口新增）

- **换样本换风格**：瓷砖样本（85 条）换成**插画师朋友圈样本**（41 条，原创插画师文案，主题偏灵感/画画/生活感悟）→ 重建画像 + 重启服务后，生成风格 4/5 完全切换（谈灵感、画画、节气），仅 1 条有轻微生意先验残留。结论：**换参考文档就能换风格**，画像和示例都来自 data/，前端上传即生效。
- **前端移除参考文档 = 归档不真删**（用户要求）：`delete_sample` 把文件移入 `data/_archive/`，可恢复；前端卡片悬停 × 移除 → 归档区显示 → 恢复按钮移回。移除后 `generator.reset_pools()` 清示例池缓存。`data/` 用 `glob("*")` 不递归，归档文件不会被读入。
- **移除后 0 样本**：自动 `clear_style()` 清画像，前端提示重新生成。
- ⚠️ 换样本/重建画像后**必须重启服务**（示例池懒加载缓存），重启后强刷页面。

### 5.3 测试注意事项（踩过的坑）

- **curl 发中文 JSON 会 500**：Git Bash 的 curl `-d '中文'` 按 GBK 编码发送，FastAPI 按 UTF-8 解析报 `UnicodeDecodeError`。**测试中文 body 必须写成 UTF-8 文件再 `-d @file.json`**（加 `-H "Content-Type: application/json; charset=utf-8"`）
- 后台跑 uvicorn 时日志重定向到文件才能看到报错堆栈
- **换样本/重建画像后必须重启服务**：示例池 `_refill_pool` 懒加载缓存，启动时装的是当时的 data/；画像文件改了但服务不重启的话，示例池还是旧样本，画像和示例打架、输出风格混乱。换样本后一定重启 uvicorn

## 6. 环境与安全（务必遵守）

- `.env` 含 DashScope API key：**绝不读取其内容、绝不提交 git**（.gitignore 已排除，提交前自查 `git status`）
- git 仓库在项目根：备份提交 `b9d49e1`（v1 时代）、画像 v2 提交 `d03a898`；**本轮归档功能 + 插画师样本 + 前端删除按钮已提交**。`style/`（画像）与 `*.old`（备份）已进 .gitignore，不入库
- 临时测试脚本放系统 Temp，用完即删
- 前端改完刷新即生效；Python 改完需重启 uvicorn（启动稍慢，等待脚本超时不算失败，直接 curl 验证）

## 7. 下一步

1. 用户多轮实测**插画师风格**下的生成效果（样本已换为插画师，画像可随时前端重新生成）
2. 测试**归档功能**：前端悬停卡片 × 移除 → 归档区 → 恢复
3. 若 humor 效果不明显：考虑补充自嘲幽默类样本（用户已有认知：补 5 条幽默型价值 > 补 20 条观念型）
4. 若后续想换回 qwen-plus 或试其他模型：改 .env `LLM_MODEL_NAME` 一行即可
5. 远期：样本量到几百条后可考虑微调模型（终极"像本人"，当前 41 条插画师样本不够）

# 瓷砖文案生成器 — 交接文档（2026-08-11 轮）

> 本窗口（2026-08-11，第三轮）会话结束交接。接手前先读本文件 + `git log`。
> 上一轮交接：`HANDOVER.md`（覆盖画像 v2 重构/纯感悟模式/换模型，历史结论仍有效，先读它再看本文件）。
> 用户是**深圳 AI 应用开发者**，偏好**中文交流**、**先讨论再开工**（动代码前先讲方案确认）。
> 本窗口主题：**前端移除/归档参考文档 + 换插画师样本验证换风格 + git 上传**。

---

## 1. 本窗口做了什么（对照旧 HANDOVER.md，增量部分）

### 1.1 前端"移除参考文档"功能（归档，不真删）— 已上线

用户需求链：前端要能去掉参考文档（不参与画像生成和案例抽取）→ 澄清"先做成移除出检索目录，不用真删"。

- 后端 `style_manager.py`：`delete_sample`（移入 `data/_archive/`，同名加时间戳）、`list_archived`、`restore_sample`；`ARCHIVE_DIR = DATA_DIR / "_archive"`
- 后端 `app.py`：`DELETE /api/sample`（移除+清示例池缓存，0 样本时 `clear_style`）、`GET /api/archived`、`POST /api/restore`；`/api/status` 返回 `archived`
- 后端 `generator.py`：`reset_pools()` 清 `_example_pool`/`_subtype_pools` 缓存
- 前端 `static/index.html`：卡片悬停显示 **×** 移除按钮 → confirm → 归档区（样本列表下方，"已移除（归档）· 文件保留，不再参与生成"）→ 恢复按钮移回
- `data/` 用 `glob("*")` **不递归**，归档文件不会被读入样本池

### 1.2 "前端看不到删除按钮"的排查（血泪教训，三层原因）

| 层 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 浏览器 | 改了 index.html 永远看到旧的 | 静态页面被缓存 | `/` 接口加 `Cache-Control: no-cache`（app.py） |
| CSS | 强刷后仍没有按钮 | `.remove` 用 `position:absolute` 但卡片 `.tile` **没设 `position:relative`**，按钮脱离卡片被甩到 body 角落（一直在，只是看不到） | `.tile` 加 `position:relative` |
| 可读性 | 按钮太淡差点没看到 | 半透明 × 看不清 | 改陶土橙圆角小按钮（橙底 `rgba(180,85,45,.10)` + 描边 + `--warn` 实心 hover 白字） |

**排查教训**：前端问题先分"缓存 / CSS / JS"三层验证。本例 curl 验证代码在页面里、服务返回对，仍看不到——根因在 CSS 定位，不是缓存不是代码缺失。

### 1.3 换参考文档 = 换风格（验证成立）

瓷砖样本（85 条）换成**插画师朋友圈样本**（41 条，原创插画师文案，主题偏灵感/画画/节气/生活感悟）→ 重建画像 + 重启服务 → 生成 4/5 完全切换，仅 1 条轻微生意先验残留。结论：**前端上传换文档 → 重建画像 → 重启服务 = 换风格**，代码零改动。

- 瓷砖原始样本备份：`data/参考文本.md.old`（**git 已忽略 `*.old`**，只留本地）
- 当前生效样本：`data/插画师朋友圈.md`

### 1.4 git 提交 + 上传 GitHub

- 最新 commit `cf34c6d`：`feat: 前端移除/归档参考文档 + 换插画师样本验证换风格 + no-cache`，已推送 origin/master
- 仓库：https://github.com/hwskunk/personalautotext
- `.gitignore` 新增 `style/`（画像构建产物）和 `*.old`（备份）——`style/tile_style.json`、`data/参考文本.md` 已从 git 索引移除（`git rm --cached`，本地文件保留）

---

## 2. 当前系统状态（2026-08-11 结束时）

| 项 | 状态 |
|---|---|
| 主模型 | **deepseek-v4-pro**（.env `LLM_MODEL_NAME` 固化；同 key 可换 qwen-plus / deepseek-v4-flash） |
| 生效样本 | `data/插画师朋友圈.md`（41 条插画师文案） |
| 画像 | `style/tile_style.json`（构建产物，**不在 git**，换样本后前端重新生成） |
| 归档 | `data/_archive/`（当前空；前端可移除/恢复） |
| 前端 | 卡片 × 移除按钮（陶土橙）+ 归档区 + 恢复按钮 + `/` no-cache |
| 服务 | 未在跑（窗口结束时 uvicorn 停掉了）；启动命令见下 |

启动：`cd "C:/Users/15613/Desktop/LangchainRAGtrain/personalaitext" && rag_env/Scripts/python -m uvicorn app:app --host 127.0.0.1 --port 8000`，访问 http://127.0.0.1:8000

---

## 3. 踩过的坑（本窗口新增，重要）

1. **前端按钮不见先查 CSS 定位**，别只归咎缓存（见 1.2）
2. **git push 经 Clash 代理时 schannel TLS 握手失败**：
   - 报错 `schannel: failed to receive handshake, SSL/TLS connection failed`
   - 解决：`git -c http.sslBackend=openssl push origin master`（临时覆盖）
   - ⚠️ **用户拒绝了把 `http.sslBackend=openssl` 持久化到全局配置**（窗口末尾拒绝过一次）——新窗口推送时若再报 schannel 错误，先用临时 `-c http.sslBackend=openssl` 覆盖，不要擅自改全局配置，必要时问用户
   - 代理：git 配置了 `http.proxy=http://127.0.0.1:7897`（Clash Verge 默认端口）。用户**平时可能关着代理**，关着时 push 报"Failed to connect ... 127.0.0.1"；直连 github.com 实测可通（200），用户开代理后走 7897
3. **换样本/重建画像后必须重启服务**（旧结论重申）：示例池 `_refill_pool` 懒加载缓存，启动时装的是当时的 `data/`；只换文件不重启，画像和示例打架、风格混乱
4. 中文 JSON 用 curl 发会 500（GBK/UTF-8）：写 UTF-8 文件再 `-d @file.json` + `charset=utf-8`（旧结论重申）

---

## 4. 下一步建议

1. **测试归档功能**：启动服务 → 前端悬停插画师卡片 → × 移除 → 确认归档区出现 → 恢复 → 重新参与生成
2. **插画师风格实测**：多轮生成看是否全切到"灵感/画画/节气"主题，无瓷砖生意先验残留
3. 之后若换回别的参考文档：上传 → 重新生成画像 → 重启服务 → 强刷页面
4. 远期：样本量到几百条后可考虑微调模型（当前插画师 41 条不够）

---

## 5. 环境与安全（务必遵守，旧结论重申）

- `.env` 含 DashScope API key：**绝不读取内容、绝不提交 git**（.gitignore 已排除；提交前自查 `git status`，本次已验证 `.env` 未被跟踪）
- 前端改完刷新即生效；Python 改完需重启 uvicorn
- 临时测试脚本放系统 Temp，用完即删

# 阿里云服务器端口放行"三件套"

> 部署环境：阿里云 ECS Linux（Alibaba Cloud Linux / CentOS 系）+ 宝塔面板
> 踩坑记录：服务起来了、端口绑定没问题，但外网死活连不上，最后发现是三层放行缺一不可。

## 一句话结论

**阿里云安全组 → 宝塔防火墙 → 服务器防火墙（firewalld）三层都要放行**，少一层端口就进不来。最容易漏的是"宝塔防火墙"这一层。

| 层 | 在哪里操作 | 作用 | 最易踩的坑 |
|---|---|---|---|
| ① 阿里云安全组 | 云控制台 | 云网络入口放行 | 规则加到了没关联实例的安全组上 |
| ② 宝塔防火墙 | 宝塔面板（安全 → 防火墙） | 面板层端口管控，**默认拦未放行端口** | 忘了这层，服务"看着正常"却连不上 |
| ③ 服务器防火墙 | 服务器命令行 | firewalld / nftables 规则落地 | 宝塔放行后忘了确认，以为没生效 |

---

## 第一步：阿里云安全组

控制台 → **实例** → 点实例名 → **实例详情** → **安全组标签页** → 记下**实际关联的安全组 ID** → 进入该组 → **入方向** → 添加规则：

```
授权策略：允许
协议类型：TCP（或"全部"）
端口范围：8000/8000
授权对象：0.0.0.0/0
优先级：1
```

**注意**：
- 一定要在**实例详情里实际关联的安全组**上操作，别在"安全组"菜单里随便找一个（配错组 = 白配）
- 保存后回入方向规则列表，**确认规则真的出现了**（光填弹窗不点保存 = 白配）

## 第二步：宝塔防火墙（最容易被忽略的一层）

登录宝塔面板（`http://服务器IP:8888`）→ 左侧 **安全 / 防火墙** → 添加放行端口：

```
端口：8000
协议：TCP
```

**为什么必须有这层**：宝塔防火墙有**独立的端口名单**，默认**只放行名单里的端口，其余一律丢弃**。即使阿里云安全组放行了，宝塔名单里没有照样进不来。

## 第三步：服务器防火墙确认

宝塔放行后，到服务器命令行确认规则真正落地：

```bash
firewall-cmd --state          # 应显示 running（宝塔会把它拉起来）
firewall-cmd --list-ports     # 应能看到 8000/tcp
```

若 firewalld 方案不可用（宝塔可能用 iptables），则：

```bash
iptables -L INPUT -n | grep 8000
```

## 验证（务必从"外部"测，不要在服务器上测自己）

**服务器上测自己公网 IP 是假象**（ECS 从内部访问自己的公网 IP 经常不通，走 NAT 回环），不能作为判断依据。

在**自己电脑**上测：

```powershell
Test-NetConnection 118.31.188.100 -Port 8000
```

```
TcpTestSucceeded : True   ← 通
```

或浏览器直接访问 `http://服务器IP:8000`。

---

## 踩坑原理（为什么"看着没问题"却连不上）

1. **`firewall-cmd --state` 显示 not running ≠ 宝塔防火墙没拦**
   - 它只代表系统 firewalld 服务没启动，宝塔防火墙是独立拦截层，一直"开着"
   - 所以"防火墙关闭"的状态下反而连不上——拦你的是宝塔，不是 firewalld

2. **`iptables -S` 显示全放行 ≠ 系统没规则（假象）**
   - 现代系统（CentOS 8+ / Alibaba Cloud Linux 3）firewalld 用 **nftables 后端**
   - legacy 的 `iptables` 命令看不到 nftables 里的规则，看着干净其实是没查到对的层

3. **配置完照样连不上 → 逐个检查**
   - 服务没起来？`ss -tlnp | grep 8000` 看是否监听、是否绑定 `0.0.0.0`（不是 `127.0.0.1`）
   - 安全组配错组？实例详情 → 安全组标签页核对 ID
   - 宝塔漏放行？宝塔面板 → 安全 → 防火墙里有没有 8000
   - 服务器防火墙？`firewall-cmd --list-ports` 有没有 8000

## 常用命令速查

```bash
# 服务监听情况（本机先确认服务活着、绑对地址）
ss -tlnp | grep 8000

# 本机验证服务正常
curl http://127.0.0.1:8000/api/status

# 系统防火墙状态与放行列表
firewall-cmd --state
firewall-cmd --list-ports
firewall-cmd --permanent --add-port=8000/tcp && firewall-cmd --reload

# 查本机真实公网 IP（确认访问的 IP 对不对）
curl ip.sb

# 本机（Windows）端口连通性测试
Test-NetConnection 服务器IP -Port 8000
```
